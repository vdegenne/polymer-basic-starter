// import {UPDATE_USER_INFORMATION} from '../actions/data';


const data = (state = {}, action) => {
  switch (action.type) {
    
    // case UPDATE_USER_INFORMATION:
    //   return {
    //     ...state,
    //     user: action.user
    //   };
    default:
      return state;
  }
}

export default data;